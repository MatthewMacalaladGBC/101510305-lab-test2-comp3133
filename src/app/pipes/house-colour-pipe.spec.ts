import { HouseColourPipe } from './house-colour-pipe';

describe('HouseColourPipe', () => {
  it('create an instance', () => {
    const pipe = new HouseColourPipe();
    expect(pipe).toBeTruthy();
  });
});
